{
	"name": "fuckingnexacro",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "npm run build:spa && npm run build:iife && vite",
		"build": "npm run build:spa && npm run build:iife",
		"build:spa": "vite build --config vite.config.js", 
		"build:iife": "vite build --config vite.config.iife.js", 
		"preview": "vite preview"
	},
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "^3.1.1",
		"svelte": "^4.2.18",
		"vite": "^5.4.19"
	}
}